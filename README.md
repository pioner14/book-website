# Многоязычный веб-сайт книги

Этот проект представляет собой многоязычный веб-сайт для онлайн-публикации книги с поддержкой русского, украинского и французского языков. Проект построен на основе Astro и использует коллекции контента для управления главами книги с поддержкой интернационализации.

## 🚀 Текущая функциональность

- **Многоязычная поддержка** (русский, украинский, французский)
- **Динамическая генерация оглавления** из коллекций контента
- **Автоматическая сортировка глав** по порядку
- **Прогресс чтения** с сохранением позиции
- **Адаптивный дизайн** с использованием Tailwind CSS
- **Поддержка шрифтов Google** для улучшения типографики

## 📁 Структура проекта

```
/
├── astro.config.mjs          # Конфигурация Astro
├── package.json             # Зависимости и скрипты
├── src/
│   ├── components/          # Компоненты Astro
│   │   ├── Header.astro     # Шапка сайта
│   │   ├── Footer.astro     # Подвал сайта
│   │   └── ReadingProgress.astro # Компонент прогресса чтения
│   ├── content/
│   │   └── book/           # Коллекция глав книги
│   │       ├── ru/         # Русские главы
│   │       ├── ua/         # Украинские главы
│   │       └── fr/         # Французские главы
│   ├── i18n/               # Файлы интернационализации
│   ├── layouts/            # Макеты страниц
│   │   └── Layout.astro
│   ├── pages/              # Страницы сайта
│   │   ├── index.astro     # Перенаправление на русский язык
│   │   └── [lang]/         # Динамические страницы по языкам
│   │       └── index.astro
│   └── styles/             # Стили
│       └── tailwind.css
└── public/                 # Статические ресурсы
```

## 🧞 Команды

Все команды выполняются из корня проекта в терминале:

| Команда                   | Действие                                           |
| :------------------------ | :-----------------------------------------------   |
| `npm install`             | Установка зависимостей                             |
| `npm run dev`             | Запуск локального сервера на `localhost:4321`      |
| `npm run build`           | Сборка продакшн-версии в папку `./dist/`           |
| `npm run preview`         | Предпросмотр сборки перед деплоем                  |
| `npm run astro ...`       | CLI команды Astro (`astro add`, `astro check`)     |

## 🌍 Добавление новых языков

Для добавления нового языка:

1. Добавьте код языка в `astro.config.mjs` в настройки i18n
2. Создайте папку с кодом языка в `src/content/book/`
3. Добавьте переводы в файлы `src/i18n/*.json`
4. Обновите логику в `src/pages/[lang]/index.astro` если необходимо

## 📚 Добавление глав

Для добавления новой главы:

1. Создайте файл `.md` в соответствующей языковой папке в `src/content/book/`
2. Укажите в frontmatter заголовок и порядковый номер:
   ```markdown
   ---
   title: "Название главы"
   order: 4
   description: "Краткое описание главы (опционально)"
   ---
   ```
3. Глава автоматически появится в оглавлении

## 🤝 Вклад в проект

1. Сделайте fork репозитория
2. Создайте ветку для новой функциональности (`git checkout -b feature/amazing-feature`)
3. Сделайте коммит изменений (`git commit -m 'Add some amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Создайте pull request

## 📄 Лицензия

Этот проект распространяется по лицензии MIT.
