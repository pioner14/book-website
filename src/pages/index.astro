---
// Главная страница теперь служит только для перенаправления
// Контент перенесен в src/pages/[lang]/index.astro
---

<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Redirecting...</title>
    <script is:inline>
      (function () {
        // Проверяем сохраненный язык
        var savedLang = localStorage.getItem("language");
        if (savedLang && ["ru", "ua", "fr"].includes(savedLang)) {
          window.location.replace("/" + savedLang + "/");
          return;
        }

        // Проверяем язык браузера
        var userLang = navigator.language || navigator.userLanguage;
        if (userLang) {
          userLang = userLang.toLowerCase();
          if (userLang.startsWith("uk") || userLang.startsWith("ua")) {
            window.location.replace("/ua/");
          } else if (userLang.startsWith("fr")) {
            window.location.replace("/fr/");
          } else {
            window.location.replace("/ru/");
          }
        } else {
          // По умолчанию
          window.location.replace("/ru/");
        }
      })();
    </script>
    <meta http-equiv="refresh" content="0;url=/ru/" />
  </head>
  <body>
    <p>
      Если перенаправление не произошло автоматически, пожалуйста, перейдите по
      <a href="/ru/">этой ссылке</a>.
    </p>
  </body>
</html>
